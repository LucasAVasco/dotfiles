{
	"Sleep infinity command": {
		"prefix": "CMD-sleep-infinity",
		"description": [
			"Set the command to sleep infinitely. Useful for debug purposes.",
			"***WARN:*** if the container does not support the `sleep infinity` command, try it with a while loop:",
			"`CMD-sleep-infinity-while-loop`"
		],
		"body": ["CMD [ \"sleep\", \"infinity\" ]"]
	},
	"Sleep infinity command (while loop)": {
		"prefix": "CMD-sleep-infinity-while-loop",
		"description": "Set the command to sleep infinitely (with a while loop). Useful for debug purposes",
		"body": [
			"CMD [ \"/bin/sh\", \"-c\", \"while [ 1 ]; do sleep 100000; done;\" ]"
		]
	}
}
