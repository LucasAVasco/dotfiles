#!/bin/bash
#
# Open the default text editor in a new window of the default terminal emulator. Create a new process in the background
#
# This is not a Linux standard functionality. I just use it in my scripts

run-detached default_text_editor_synced "$@"
