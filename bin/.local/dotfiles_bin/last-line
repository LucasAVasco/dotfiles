#!/bin/bash
#
# Return the last line of standard input.

set -e

source ~/.local/lib/dotfiles/bash/help.sh

help_handle n "$@" << EOF
	Return the last line of the provided file or standard input.

	Usage:
		last-line [file]
EOF

tail -n 1 "$@"
