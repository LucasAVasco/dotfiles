#!/bin/bash
#
# Try running a command and echo the exit code if it fails.
#
# Will not exit with a non-zero exit code.

set -e

"$@" || echo -e "\n\u001b[1;31mCommand failed with exit code\u001b[0m: $?"
